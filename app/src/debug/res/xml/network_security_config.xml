<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may use a temporary relay on a trusted development LAN. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

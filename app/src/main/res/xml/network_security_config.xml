<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Cleartext is permitted app-wide so self-hosted image/LLM backends on
      the LAN are reachable (ComfyUI at http://192.168.x.x:8188 and friends).
      Remote API endpoints (OpenAI, Anthropic, Gemini, ...) always use their
      HTTPS URLs, which are unaffected by this setting.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
